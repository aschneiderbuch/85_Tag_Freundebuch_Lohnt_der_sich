


// damit nicht überall map geschrieben werden muss
// es wird mit den props das array und die Elemente=items übergeben

/* 
import React from 'react'

export const ListeArrayMap = ( {array, ElementItem } ) => {
  return (
    <section className='listeArrayMap'>

{array.map( (item, key ) =>  <ElementItem key={key} item={item} > </ElementItem> ) }

    </section>
  )
}
 */